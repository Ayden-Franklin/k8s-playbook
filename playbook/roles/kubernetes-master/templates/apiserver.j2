###
## kubernetes system config
##
## The following values are used to configure the kube-apiserver
##
#
## The address on the local server to listen to.
KUBE_API_ADDRESS="--advertise-address={{ MASTER_IP }} --bind-address={{ MASTER_IP }} --insecure-bind-address={{ MASTER_IP }}"
#
## The port on the local server to listen on.
#KUBE_API_PORT="--port=8080"
#
## Port minions listen on
#KUBELET_PORT="--kubelet-port=10250"
#
## Comma separated list of nodes in the etcd cluster
KUBE_ETCD_SERVERS="--etcd-servers={{ ETCD_ENDPOINTS  }}"
#
## Address range to use for services
KUBE_SERVICE_ADDRESSES="--service-cluster-ip-range={{ SERVICE_CLUSTER_IP }}"
#
## default admission control policies
KUBE_ADMISSION_CONTROL="--admission-control=ServiceAccount,NamespaceLifecycle,NamespaceExists,LimitRanger,ResourceQuota,NodeRestriction"
#
## Add your own!
KUBE_API_ARGS="--allow-privileged=true --authorization-mode=Node,RBAC --runtime-config=rbac.authorization.k8s.io/v1beta1 --kubelet-https=true --token-auth-file=/etc/kubernetes/token.csv --service-node-port-range=30000-32767 --tls-cert-file={{ kube_ca_dir}}/kubernetes.pem --tls-private-key-file={{ kube_ca_dir}}/kubernetes.key --client-ca-file={{ kube_ca_dir}}/ca.pem --service-account-key-file={{ kube_ca_dir}}/ca.key --etcd-cafile={{ etcd_ca_dir}}/ca.pem --etcd-certfile={{ etcd_ca_dir}}/etcd.pem --etcd-keyfile={{ etcd_ca_dir}}/etcd.key --enable-swagger-ui=true --apiserver-count=3 --audit-log-maxage=30 --audit-log-maxbackup=3 --audit-log-maxsize=100 --audit-log-path=/var/lib/audit.log --event-ttl=1h"
#KUBE_API_ARGS="--insecure-bind-address=0.0.0.0  --kubelet-https=false --service-node-port-range=30000-32767 --enable-swagger-ui=true --apiserver-count=1"
